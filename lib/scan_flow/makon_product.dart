import 'package:room_scan_kit/scan_flow/scan_flow.dart';

/// Explicit Makon product context — never infer from UI or bundle name.
const ScanEntryConfiguration kMakonScanEntry = ScanEntryConfiguration(
  product: ProductContext.makon3D,
);
